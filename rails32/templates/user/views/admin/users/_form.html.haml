= simple_form_for [:admin, @user] do |f|
	= f.input :email
	= f.input :password
	= f.input :first_name
	= f.input :last_name
	= f.input :role, :collection => User::ROLES, :include_blank => false
	= f.input :activated
	= f.submit